# AGENTS.md

> 本文件为通用 Java 企业项目代码规范摘要，用于约束生成、修改和重构代码时的实现风格与工程习惯。

- 严格遵守《阿里巴巴 Java 开发手册》中的通用规范；如与现有项目约定冲突，优先遵循项目内已明确的规范。

## 1. 分层与职责

- 遵守标准分层架构：Controller、Service、Mapper/Repository 各司其职。
- Controller 负责接收参数、调用 Service、返回结果，不直接访问 Mapper。
- Service 负责业务编排与事务控制，不承担持久层实现细节。
- Mapper/Repository 只负责数据访问，不编写业务逻辑。
- DTO、VO、Entity 仅作为数据承载对象，不编写业务逻辑。
- 工具类只封装通用能力，不写具体业务规则；新增工具方法前，优先复用项目内已有工具类与公共方法。

## 2. 命名与结构

- 包名全小写，按职责分包，避免在同一包中混放不同层级文件。
- 类名使用大驼峰，方法名和变量名使用小驼峰，常量使用全大写下划线。
- 接口、实现类、Mapper、Controller 命名清晰明确，如 `XxxService`、`XxxServiceImpl`、`XxxMapper`、`XxxController`。
- 禁止含糊命名、随意缩写和魔法数字，命名应做到见名知意。
- 新增工具类时先遵循现有项目命名与组织方式；在无既有约束时，可统一以 `Util` 结尾，避免使用带业务含义的命名。
- 工具类与基础库优先遵循现有项目依赖选型；在无既有约束时，优先复用 Apache Commons、Guava、Jackson 等通用生态，无明确原因时尽量不引入 Hutool、Fastjson 等额外工具依赖。
- 新增 Java 类、接口、枚举、工具类、配置类时，文件头使用以下作者注释格式：

```java
/**
* @author chenwenhao
* @date：自动填写当前日期
* @desc：类的功能说明
  */
```

## 3. 代码风格与基础规范

- 统一使用 UTF-8 编码、四个空格缩进，禁止使用 Tab。
- 单行长度建议不超过 120 个字符，单个方法应保持职责单一。
- 代码实现优先遵循现有项目的风格、工具用法和组织方式，避免在局部引入突兀的新写法。
- 依照现有项目的注入风格保持一致；在新模块或无既有约束时，优先使用构造器注入，避免因局部风格突变影响一致性。
- `String` 比较优先沿用项目现有写法；在无既有约束时，优先使用 `StringUtils.equals`、`StringUtils.equalsAny` 等工具方法，禁止使用 `==` 比较内容。
- `BigDecimal` 比较使用 `compareTo`，构造优先沿用项目现有写法；在无既有约束时，优先使用 `BigDecimal.valueOf` 或字符串构造。
- 集合判空优先使用 `CollectionUtils.isEmpty`、`MapUtils.isEmpty` 等通用工具方法。
- 返回集合或容器对象时禁止返回 `null`，优先返回空集合。
- 统一使用接口类型声明集合变量，如 `List`、`Map`、`Set`。
- 使用 `Optional` 时仅用于返回值，不作为类成员变量。
- 需要事务控制的方法使用 `@Transactional(rollbackFor = Exception.class)`。
- 禁止直接使用 `new Thread()` 创建线程，统一使用显式配置的线程池。
- 仅做必要的非空判断与状态校验；无明确业务依据时，不额外补充默认值、兜底返回或容错分支来掩盖异常数据。

## 4. 日志、异常与注释

- 禁止使用 `System.out.println`，统一使用 `Slf4j` 记录日志。
- 日志必须使用占位符写法，禁止字符串拼接。
- 捕获异常后必须明确处理、记录日志或转换为业务异常，禁止空 `catch`。
- 抛出异常时应补充必要上下文，避免直接抛出无语义的通用异常。
- 注释统一使用中文，说明业务意图或实现原因，不解释基础语法。
- 行级注释必须写在代码上一行，禁止行尾注释。
- 对复杂分支、关键判断、重要赋值、方法调用链和 Lambda 表达式补充简洁注释。
- 方法注释和类注释应描述职责、输入输出和核心用途，保持与代码同步更新。

## 5. 数据库与 SQL 规范

- 数据访问统一通过 Mapper、Repository 或 ORM 能力实现，禁止在 Service 中拼接 SQL。
- 禁止 `SELECT *`，只查询实际需要的字段。
- 必须使用预编译参数，禁止字符串拼接 SQL 条件。
- 涉及分页、排序、过滤条件时，必须校验参数合法性。
- 编写 SQL 时优先考虑数据库版本兼容性，避免使用不确定兼容性的特性。
- Mapper XML 保持统一缩进，SQL 关键字使用大写。
- 涉及 `insert`、`update`、`delete`、逻辑删除、批量更新等操作时，必须在语句上一行添加中文业务注释，说明更新目的和作用范围。

```java
// 将指定条件范围内的数据更新为已失效状态
service.lambdaUpdate()
    .eq(DemoEntity::getType, type)
    .in(DemoEntity::getId, idList)
    .set(DemoEntity::getStatus, status)
    .update();
```

## 6. 限制项与禁止项

- 禁止新增 Magic String。凡是参与条件分支、状态流转、类型判断、规则判断的字符串字面量，必须收敛为常量、枚举或值对象。
- 静态导入不是默认写法，仅在明显提升可读性且来源清晰时使用，禁止大面积静态导入常量或工具方法。
- 默认不创建内部类。仅在枚举常量体、短生命周期适配器或确有必要的封装场景下谨慎使用。
- 需要复用、测试、注入容器、参与序列化或对外暴露的类型，必须定义为顶级类。
- 禁止在生产代码中引入无明确业务依据的兜底分支、默认返回值或掩盖异常的降级逻辑。
- 禁止 silently 修改业务语义明确的状态值、时间单位、金额逻辑和条件含义。

## 7. 测试计划规范

- 测试计划优先服务于真实业务链路验证和代码调试，不默认生成大量 `MockBean`、桩对象或依赖交互测试。
- 对于 Spring Boot 业务方法，优先在测试类中注入真实 Bean，直接调用目标方法，便于开发者跟随断点调试。
- 当主要风险在需求理解偏差而非代码实现时，优先澄清需求，不通过补充大量测试替代需求确认。
